angular.module('DigiClubs').factory('Authenticate', [ 
	function(){
		
		return{auth:{}} ;
	}]);

// angular.module('users').factory('Authentication', ['$window',
//   function ($window) {
//     var auth = {
//       user: $window.user
//     };

//     return auth;
//   }
// ]);